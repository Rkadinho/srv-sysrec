spring:
  application:
    name: srv-sysRec
  datasource:
    url: jdbc:postgressql://loalhost:5432/sysRec
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

server:
  port: 8081

security:
  jwt:
    secret: minha-chave-jwt-super-secreta-para-bff-e-srv-2026-hexagonal